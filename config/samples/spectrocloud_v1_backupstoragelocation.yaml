apiVersion: spectrocloud.spectrocloud.com/v1
kind: BackupStorageLocation
metadata:
  labels:
    app.kubernetes.io/name: sc
    app.kubernetes.io/managed-by: kustomize
  name: backupstoragelocation-sample
spec:
  # TODO(user): Add fields here
