apiVersion: spectrocloud.spectrocloud.com/v1
kind: ClusterAks
metadata:
  labels:
    app.kubernetes.io/name: sc
    app.kubernetes.io/managed-by: kustomize
  name: clusteraks-sample
spec:
  # TODO(user): Add fields here
