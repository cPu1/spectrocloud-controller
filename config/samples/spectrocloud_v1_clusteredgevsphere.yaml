apiVersion: spectrocloud.spectrocloud.com/v1
kind: ClusterEdgeVsphere
metadata:
  labels:
    app.kubernetes.io/name: sc
    app.kubernetes.io/managed-by: kustomize
  name: clusteredgevsphere-sample
spec:
  # TODO(user): Add fields here
