apiVersion: spectrocloud.spectrocloud.com/v1
kind: ClusterOpenstack
metadata:
  labels:
    app.kubernetes.io/name: sc
    app.kubernetes.io/managed-by: kustomize
  name: clusteropenstack-sample
spec:
  # TODO(user): Add fields here
