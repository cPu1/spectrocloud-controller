apiVersion: spectrocloud.spectrocloud.com/v1
kind: ClusterImport
metadata:
  labels:
    app.kubernetes.io/name: sc
    app.kubernetes.io/managed-by: kustomize
  name: clusterimport-sample
spec:
  # TODO(user): Add fields here
