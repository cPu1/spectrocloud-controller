apiVersion: spectrocloud.spectrocloud.com/v1
kind: ClusterMaas
metadata:
  labels:
    app.kubernetes.io/name: sc
    app.kubernetes.io/managed-by: kustomize
  name: clustermaas-sample
spec:
  # TODO(user): Add fields here
