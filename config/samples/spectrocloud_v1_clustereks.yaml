apiVersion: spectrocloud.spectrocloud.com/v1
kind: ClusterEks
metadata:
  labels:
    app.kubernetes.io/name: sc
    app.kubernetes.io/managed-by: kustomize
  name: clustereks-sample
spec:
  # TODO(user): Add fields here
