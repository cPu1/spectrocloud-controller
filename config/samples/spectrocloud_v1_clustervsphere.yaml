apiVersion: spectrocloud.spectrocloud.com/v1
kind: ClusterVsphere
metadata:
  labels:
    app.kubernetes.io/name: sc
    app.kubernetes.io/managed-by: kustomize
  name: clustervsphere-sample
spec:
  # TODO(user): Add fields here
